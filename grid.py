class GRID:
	def __init__(self):
		self.items = []
		self.len = 0 

	def push(self,*items):
		for element in item:
			lista.append(element)
		self.items.append(",".join(lista))
		self.len +=1

	def show(self):
		i = 0 
		for item in items:
			print(item)

# la idea seria pasar esto con listas enlazadas simples